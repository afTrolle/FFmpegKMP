# iOS Studio launcher

Open `FFmpegKMPStudio.xcodeproj`, choose the `FFmpegKMPStudio` scheme and an iOS
15+ simulator, then run. The Xcode build phase invokes
`:samples:studio:embedAndSignAppleFrameworkForXcode`; locally generated Apple
FFmpeg archives must exist as described in `../../docs/native-builds.md`.
